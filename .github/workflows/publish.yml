name: Publish Factorio Mod

on: [push]

jobs:
  publish-mod:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Validate Mod
        uses: TheBrutalX/factorio-mod-uploader-action@0.0.9
        with:
          action: validate